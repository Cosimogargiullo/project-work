name: Render Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Call keep-alive endpoint
        run: |
          curl --fail --show-error --silent https://carehub-backend-qitv.onrender.com/api/ping
